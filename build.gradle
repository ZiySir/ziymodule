plugins {
	id 'java-basic'
	id 'com.palantir.baseline-config'
	id 'baseline'
	id 'git-hooks'
}

allprojects {
	group = providers.gradleProperty('groupId').getOrElse('org.demo')
	version = '0.0.1'
}

gitHooks {
	prePushCommand = './gradlew spotlessCheck check --no-parallel -x test --no-daemon'
}
